# 🚗 Ride Hailing App - Complete Architecture & Flow Guide

## 📋 Table of Contents
1. [System Architecture](#system-architecture)
2. [Database Architecture](#database-architecture)
3. [Backend Architecture](#backend-architecture)
4. [Frontend Architecture](#frontend-architecture)
5. [Complete Flow Diagrams](#complete-flow-diagrams)
6. [API Endpoints](#api-endpoints)
7. [Frontend-Backend Connection](#frontend-backend-connection)

---

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    RIDE HAILING SYSTEM                     │
└─────────────────────────────────────────────────────────────┘
                              │
                ┌─────────────┴─────────────┐
                │                           │
        ┌───────▼────────┐         ┌───────▼────────┐
        │   FRONTEND     │         │    BACKEND     │
        │   (Flutter)    │◄───────►│  (Spring Boot) │
        │                │  HTTP   │                │
        │                │  WebSocket│                │
        └───────┬────────┘         └───────┬────────┘
                │                           │
                │                           │
        ┌───────▼────────┐         ┌───────▼────────┐
        │  Mobile Device │         │   PostgreSQL   │
        │   (Android)    │         │    Database    │
        └───────────────┘         └────────────────┘
```

---

## 🗄️ Database Architecture

### **Database Schema**

```
┌─────────────────────────────────────────────────────────────┐
│                      DATABASE: ridehailing                  │
└─────────────────────────────────────────────────────────────┘

┌──────────────┐
│    roles     │
├──────────────┤
│ role_id (PK) │
│ role_name    │ ──┐
│ description  │   │
└──────────────┘   │
                   │ 1:N
┌──────────────┐   │
│    users     │◄──┘
├──────────────┤
│ user_id (PK) │ ──┐
│ email        │   │
│ password_hash│   │
│ phone_number │   │
│ first_name   │   │
│ last_name    │   │
│ role_id (FK) │   │
└──────────────┘   │
                   │ 1:1
        ┌──────────┴──────────┐
        │                     │
┌───────▼──────┐    ┌─────────▼──────┐
│   riders     │    │    drivers     │
├──────────────┤    ├────────────────┤
│ rider_id (PK)│    │ driver_id (PK) │ ──┐
│ rating       │    │ license_number │   │
│ total_rides  │    │ rating         │   │
└──────────────┘    │ status         │   │
                    │ total_earnings │   │
                    └────────────────┘   │
                                         │ 1:N
                    ┌────────────────────┘
                    │
            ┌───────▼──────┐
            │   vehicles   │
            ├──────────────┤
            │ vehicle_id   │
            │ driver_id(FK)│
            │ model        │
            │ make         │
            │ license_plate│
            └──────────────┘

┌──────────────┐
│    rides     │
├──────────────┤
│ ride_id (PK) │
│ rider_id(FK) │──► riders
│ driver_id(FK)│──► drivers
│ vehicle_id(FK)│──► vehicles
│ pickup_lat   │
│ pickup_lng   │
│ dropoff_lat  │
│ dropoff_lng  │
│ status       │
│ fare         │
└──────────────┘
```

### **Entity Relationships**

```
roles (1) ──┐
            │
            ├──► users (N) ──┬──► riders (1:1)
            │                │
            │                └──► drivers (1:1) ──► vehicles (1:N)
            │
            └──► rides (N) ──┬──► riders
                             ├──► drivers
                             └──► vehicles
```

---

## 🔧 Backend Architecture

### **Layered Architecture**

```
┌─────────────────────────────────────────────────────────┐
│                    PRESENTATION LAYER                     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │
│  │AuthController│  │RideController│  │WebSocketCtrl │   │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘   │
└─────────┼──────────────────┼──────────────────┼──────────┘
          │                  │                  │
          │ HTTP/WebSocket   │                  │
┌─────────▼──────────────────▼──────────────────▼──────────┐
│                      SERVICE LAYER                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │
│  │AuthService   │  │RideService   │  │JwtService    │   │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘   │
└─────────┼──────────────────┼──────────────────┼──────────┘
          │                  │                  │
          │ Business Logic   │                  │
┌─────────▼──────────────────▼──────────────────▼──────────┐
│                    REPOSITORY LAYER                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │
│  │UserRepository│  │RideRepository│  │DriverRepo │   │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘   │
└─────────┼──────────────────┼──────────────────┼──────────┘
          │                  │                  │
          │ JPA/Hibernate     │                  │
┌─────────▼──────────────────▼──────────────────▼──────────┐
│                    DATABASE LAYER                          │
│                  PostgreSQL Database                       │
└────────────────────────────────────────────────────────────┘
```

### **Backend Components**

#### **1. Controllers (REST API)**
```
AuthController
├── POST /api/v1/auth/register/rider
├── POST /api/v1/auth/register/driver
├── POST /api/v1/auth/login
├── POST /api/v1/auth/refresh
└── POST /api/v1/auth/logout

RideController
├── POST /api/v1/rides
├── GET  /api/v1/rides/{id}
├── PUT  /api/v1/rides/{id}/accept
└── PUT  /api/v1/rides/{id}/complete

WebSocketController
└── WS /ws?token={token}
```

#### **2. Services (Business Logic)**
```
AuthenticationService
├── registerRider()
│   ├── Validate input
│   ├── Hash password (BCrypt)
│   ├── Create User
│   └── Create Rider
│
├── registerDriver()
│   ├── Validate input
│   ├── Hash password (BCrypt)
│   ├── Create User
│   ├── Create Driver
│   └── Create Vehicle
│
└── login()
    ├── Find user by email
    ├── Verify password (BCrypt)
    └── Generate JWT tokens

RideService
├── createRide()
├── acceptRide()
├── startRide()
└── completeRide()

JwtService
├── generateAccessToken()
├── generateRefreshToken()
└── validateToken()
```

#### **3. Repositories (Data Access)**
```
UserRepository
├── findByEmail()
├── existsByEmail()
└── existsByPhoneNumber()

RiderRepository
├── save()
└── findById()

DriverRepository
├── save()
├── findByLicenseNumber()
└── findById()

VehicleRepository
├── save()
├── findByLicensePlate()
└── findByDriverId()

RideRepository
├── save()
├── findById()
└── findByRiderId()
```

---

## 📱 Frontend Architecture

### **Flutter Architecture (MVVM Pattern)**

```
┌─────────────────────────────────────────────────────────┐
│                      UI LAYER                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │LoginScreen   │  │HomeScreen    │  │BookRideScreen│  │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘  │
└─────────┼──────────────────┼──────────────────┼──────────┘
          │                  │                  │
          │ User Actions      │                  │
┌─────────▼──────────────────▼──────────────────▼──────────┐
│                    STATE LAYER (Provider)                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │
│  │AuthProvider  │  │LocationState │  │RideProvider  │   │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘   │
└─────────┼──────────────────┼──────────────────┼──────────┘
          │                  │                  │
          │ Business Logic    │                  │
┌─────────▼──────────────────▼──────────────────▼──────────┐
│                    SERVICE LAYER                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │
│  │AuthService   │  │LocationService│ │RideService   │   │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘   │
│         │                  │                  │            │
│  ┌──────▼───────┐  ┌──────▼───────┐  ┌──────▼───────┐  │
│  │WebSocketService│ │Geolocator    │  │HTTP Client   │  │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘   │
└─────────┼──────────────────┼──────────────────┼──────────┘
          │                  │                  │
          │ HTTP/WebSocket    │ GPS              │
┌─────────▼──────────────────▼──────────────────▼──────────┐
│                    BACKEND API                             │
└────────────────────────────────────────────────────────────┘
```

### **Frontend Components**

#### **1. Screens**
```
LoginScreen
├── Email input
├── Password input
└── Login button

RegisterScreen
├── Role selection (Rider/Driver)
├── Basic info (email, password, phone, name)
└── Submit button

DriverDetailsScreen (if Driver)
├── License number
├── Vehicle model
└── License plate

HomeScreen
├── Map display
├── Location marker
├── User profile
└── Book Ride button

BookRideScreen
├── Pickup location (auto-filled)
├── Dropoff location
└── Submit button
```

#### **2. Providers (State Management)**
```
AuthProvider
├── user (User?)
├── accessToken (String?)
├── isLoading (bool)
├── error (String?)
├── login()
├── register()
├── logout()
└── checkAuthStatus()
```

#### **3. Services**
```
AuthService
├── login(LoginRequest)
├── registerRider(RegisterRequest)
├── registerDriver(RegisterRequest)
└── refreshToken(String)

LocationService
├── getCurrentLocation()
├── getLocationStream()
├── requestPermission()
└── clearCache()

WebSocketService
├── connect(String token)
├── disconnect()
├── sendLocationUpdate()
└── messageStream

RideService
├── createRide(RideRequest)
└── getRideDetails(String rideId)
```

#### **4. Models**
```
User
├── userId
├── email
└── role

AuthResponse
├── accessToken
├── refreshToken
└── user

LocationModel
├── latitude
├── longitude
└── timestamp

RideRequest
├── pickupLocation
└── dropoffLocation
```

---

## 🔄 Complete Flow Diagrams

### **1. User Registration Flow**

```
┌─────────────┐
│ User Opens  │
│    App      │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│RegisterScreen│
└──────┬──────┘
       │
       ▼
┌─────────────────┐
│ Select Role      │
│ (Rider/Driver)   │
└──────┬───────────┘
       │
       ▼
┌─────────────────┐
│ Enter Basic Info│
│ - Email         │
│ - Password      │
│ - Phone         │
│ - Name          │
└──────┬──────────┘
       │
       ▼
┌─────────────────┐      NO
│ Is Driver? ─────┼───────┐
└──────┬──────────┘       │
       │ YES              │
       ▼                  │
┌─────────────────┐       │
│DriverDetails    │       │
│Screen           │       │
│ - License #     │       │
│ - Vehicle Model │       │
│ - License Plate │       │
└──────┬──────────┘       │
       │                  │
       ▼                  │
┌─────────────────┐       │
│ Submit Form     │◄──────┘
└──────┬──────────┘
       │
       ▼
┌─────────────────────────────────┐
│ Frontend: AuthService           │
│ registerRider/registerDriver()  │
└──────┬──────────────────────────┘
       │
       │ HTTP POST
       │ /api/v1/auth/register/rider
       │ or /register/driver
       ▼
┌─────────────────────────────────┐
│ Backend: AuthController         │
│ registerRider/registerDriver()   │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ Backend: AuthenticationService   │
│ registerRider()                  │
│ ├── Validate input              │
│ ├── Check email exists          │
│ ├── Hash password (BCrypt)      │
│ ├── Create User entity          │
│ └── Create Rider entity         │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ Backend: UserRepository         │
│ save(user)                      │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ Database: INSERT INTO users      │
│ INSERT INTO riders              │
└──────┬──────────────────────────┘
       │
       │ Success Response
       ▼
┌─────────────────────────────────┐
│ Backend: Return 201 Created     │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ Frontend: Auto-login            │
│ login(email, password)           │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ Backend: Generate JWT tokens     │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ Frontend: Save tokens & user    │
│ Navigate to HomeScreen           │
└─────────────────────────────────┘
```

### **2. User Login Flow**

```
┌─────────────┐
│ User Opens  │
│    App      │
└──────┬──────┘
       │
       ▼
┌─────────────────┐
│ AuthWrapper     │
│ checks stored   │
│ tokens          │
└──────┬──────────┘
       │
       ├─── Token exists? ──YES──► HomeScreen
       │
       NO
       ▼
┌─────────────┐
│LoginScreen  │
└──────┬──────┘
       │
       ▼
┌─────────────────┐
│ Enter Credentials│
│ - Email          │
│ - Password       │
└──────┬───────────┘
       │
       ▼
┌─────────────────────────────────┐
│ Frontend: AuthProvider.login()   │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ Frontend: AuthService.login()    │
│ POST /api/v1/auth/login          │
└──────┬──────────────────────────┘
       │
       │ HTTP POST
       │ {email, password}
       ▼
┌─────────────────────────────────┐
│ Backend: AuthController         │
│ /api/v1/auth/login              │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ Backend: AuthenticationService  │
│ login()                          │
│ ├── Find user by email           │
│ ├── Verify password (BCrypt)     │
│ └── Generate JWT tokens          │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ Backend: JwtService             │
│ generateAccessToken()            │
│ generateRefreshToken()           │
└──────┬──────────────────────────┘
       │
       │ Response: {accessToken, refreshToken, user}
       ▼
┌─────────────────────────────────┐
│ Frontend: AuthProvider           │
│ _saveAuthData()                  │
│ ├── Save tokens (Secure Storage) │
│ ├── Save user data               │
│ └── Set state                    │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ Navigate to HomeScreen           │
└─────────────────────────────────┘
```

### **3. Location Tracking Flow**

```
┌─────────────────────────────────┐
│ HomeScreen Initializes          │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ _initializeLocationTracking()    │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ LocationService                  │
│ requestPermission()              │
└──────┬──────────────────────────┘
       │
       ├── Permission granted? ──NO──► Show error
       │
       YES
       ▼
┌─────────────────────────────────┐
│ LocationService                  │
│ getCurrentLocation()             │
└──────┬──────────────────────────┘
       │
       │ GPS
       ▼
┌─────────────────────────────────┐
│ Get GPS Coordinates              │
│ (12.9716, 77.5946) - Bangalore   │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ Update Map Marker                │
│ Display on MapScreen             │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ Start Location Stream            │
│ getLocationStream()              │
│ (Updates every 10s or 10m moved)│
└──────┬──────────────────────────┘
       │
       │ Continuous Updates
       ▼
┌─────────────────────────────────┐
│ WebSocketService                 │
│ connect(accessToken)             │
└──────┬──────────────────────────┘
       │
       │ WebSocket: ws://localhost:8080/ws?token=...
       ▼
┌─────────────────────────────────┐
│ Backend: WebSocketController    │
│ /ws endpoint                    │
│ Authenticate via JWT token       │
└──────┬──────────────────────────┘
       │
       │ Connection established
       ▼
┌─────────────────────────────────┐
│ Frontend: Send Location Update  │
│ sendLocationUpdate()             │
│ {userId, latitude, longitude}    │
└──────┬──────────────────────────┘
       │
       │ WebSocket Message
       ▼
┌─────────────────────────────────┐
│ Backend: Receive Location       │
│ Store/Process location data      │
│ (Can match with nearby drivers)  │
└─────────────────────────────────┘
```

### **4. Book Ride Flow**

```
┌─────────────────────────────────┐
│ User on HomeScreen               │
│ Taps "Book a Ride" button        │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ Navigate to BookRideScreen       │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ BookRideScreen                   │
│ ├── Auto-fill pickup (GPS)      │
│ └── User enters dropoff         │
└──────┬──────────────────────────┘
       │
       │ Optional: Tap map icon
       ▼
┌─────────────────────────────────┐
│ MapScreen (for selection)        │
│ User taps on map                 │
│ Returns coordinates              │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ User submits ride request        │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ Frontend: RideService            │
│ createRide(RideRequest)          │
│ POST /api/v1/rides               │
└──────┬──────────────────────────┘
       │
       │ HTTP POST
       │ Authorization: Bearer {token}
       │ {pickupLocation, dropoffLocation}
       ▼
┌─────────────────────────────────┐
│ Backend: RideController          │
│ /api/v1/rides                    │
│ Validate JWT token               │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ Backend: RideService            │
│ createRide()                     │
│ ├── Get rider from token        │
│ ├── Calculate estimated fare    │
│ ├── Calculate distance          │
│ └── Create Ride entity          │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ Backend: RideRepository         │
│ save(ride)                       │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ Database: INSERT INTO rides      │
│ Status: PENDING                  │
└──────┬──────────────────────────┘
       │
       │ Response: {rideId, status, estimatedFare}
       ▼
┌─────────────────────────────────┐
│ Frontend: Show ride status       │
│ Display ride details             │
└─────────────────────────────────┘
```

### **5. App Startup Flow**

```
┌─────────────────────────────────┐
│ User Opens App                   │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ main() function                  │
│ runApp(RiderApp())               │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ Create AuthProvider              │
│ checkAuthStatus()                │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ TokenStorageService              │
│ getAccessToken()                 │
│ getUserData()                    │
└──────┬──────────────────────────┘
       │
       ├── Token exists? ──NO──► LoginScreen
       │
       YES
       ▼
┌─────────────────────────────────┐
│ Restore User Data                │
│ Set _accessToken                 │
│ Set _user                        │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ AuthWrapper                      │
│ isAuthenticated = true           │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ Navigate to HomeScreen           │
│ Initialize location tracking     │
│ Connect WebSocket                │
└─────────────────────────────────┘
```

---

## 🔌 Frontend-Backend Connection

### **Network Architecture**

```
┌─────────────────────────────────────────────────────────┐
│              PHYSICAL DEVICE (USB)                      │
│  ┌──────────────────────────────────────────────────┐  │
│  │            Flutter App                           │  │
│  │  ┌──────────────┐  ┌──────────────┐             │  │
│  │  │AuthService   │  │WebSocketService│            │  │
│  │  └──────┬───────┘  └──────┬───────┘             │  │
│  │         │                 │                      │  │
│  │         │ HTTP            │ WebSocket            │  │
│  │         │ localhost:8080  │ ws://localhost:8080 │  │
│  │         │                 │                      │  │
│  └─────────┼─────────────────┼──────────────────────┘  │
└────────────┼─────────────────┼──────────────────────────┘
             │                 │
             │ ADB Port Forwarding
             │ adb reverse tcp:8080 tcp:8080
             │
┌────────────▼─────────────────▼──────────────────────────┐
│              COMPUTER (localhost)                        │
│  ┌──────────────────────────────────────────────────┐    │
│  │         Spring Boot Backend                      │    │
│  │  ┌──────────────┐  ┌──────────────┐            │    │
│  │  │AuthController│  │WebSocketCtrl │            │    │
│  │  └──────┬───────┘  └──────┬───────┘            │    │
│  │         │                 │                     │    │
│  └─────────┼─────────────────┼─────────────────────┘    │
│            │                 │                           │
│            │                 │                           │
│  ┌─────────▼─────────────────▼─────────────────────┐    │
│  │         PostgreSQL Database                      │    │
│  │  Port: 5433                                     │    │
│  └─────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────┘
```

### **Connection Methods**

#### **1. Emulator Connection**
```
Flutter App (Emulator)
    │
    │ HTTP: http://10.0.2.2:8080
    │ (10.0.2.2 = host machine's localhost)
    │
    ▼
Spring Boot (localhost:8080)
```

#### **2. Physical Device (USB) - ADB Forwarding**
```
Flutter App (Physical Device)
    │
    │ HTTP: http://localhost:8080
    │ WebSocket: ws://localhost:8080
    │
    │ ADB Port Forwarding
    │ adb reverse tcp:8080 tcp:8080
    │
    ▼
Spring Boot (localhost:8080)
```

#### **3. Physical Device (Network)**
```
Flutter App (Physical Device)
    │
    │ HTTP: http://192.168.0.125:8080
    │ (Computer's IP address)
    │
    │ Same Wi-Fi Network
    │
    ▼
Spring Boot (192.168.0.125:8080)
```

---

## 📡 API Endpoints

### **Authentication Endpoints**

```
POST /api/v1/auth/register/rider
Request:
{
  "email": "user@example.com",
  "password": "password123",
  "phoneNumber": "+1234567890",
  "firstName": "John",
  "lastName": "Doe"
}
Response: 201 Created
{
  "message": "Rider registered successfully",
  "userId": "uuid"
}

POST /api/v1/auth/register/driver
Request:
{
  "email": "driver@example.com",
  "password": "password123",
  "phoneNumber": "+1234567890",
  "firstName": "Jane",
  "lastName": "Smith",
  "licenseNumber": "DL123456",
  "vehicleModel": "Toyota Camry",
  "vehiclePlate": "ABC1234"
}
Response: 201 Created

POST /api/v1/auth/login
Request:
{
  "email": "user@example.com",
  "password": "password123"
}
Response: 200 OK
{
  "accessToken": "jwt_token",
  "refreshToken": "refresh_token",
  "tokenType": "Bearer",
  "expiresIn": 3600,
  "user": {
    "userId": "uuid",
    "email": "user@example.com",
    "role": "RIDER"
  }
}
```

### **Ride Endpoints**

```
POST /api/v1/rides
Headers: Authorization: Bearer {token}
Request:
{
  "pickupLocation": {
    "latitude": 12.9716,
    "longitude": 77.5946,
    "address": "Bangalore"
  },
  "dropoffLocation": {
    "latitude": 12.9352,
    "longitude": 77.6273,
    "address": "Koramangala"
  }
}
Response: 201 Created
{
  "rideId": "uuid",
  "status": "PENDING",
  "estimatedFare": 150.00,
  "estimatedArrivalTime": 15
}
```

### **WebSocket Endpoint**

```
WS /ws?token={accessToken}
Connection: WebSocket
Authentication: JWT token in query parameter

Messages:
├── Send: {"latitude": 12.9716, "longitude": 77.5946}
└── Receive: {"type": "location:updated", "data": {...}}
```

---

## 🔐 Security Flow

```
┌─────────────────────────────────┐
│ User Login                      │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ Backend: Verify Password         │
│ BCrypt.compare(plain, hash)      │
└──────┬──────────────────────────┘
       │
       ├── Valid? ──NO──► Error 401
       │
       YES
       ▼
┌─────────────────────────────────┐
│ Backend: Generate JWT            │
│ ├── Access Token (1 hour)        │
│ └── Refresh Token (7 days)       │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ Frontend: Store in Secure Storage│
│ (Encrypted)                      │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ Subsequent Requests              │
│ Header: Authorization: Bearer   │
│ {accessToken}                    │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ Backend: JwtAuthenticationFilter│
│ Validate token                   │
│ Extract user info                │
└──────┬──────────────────────────┘
       │
       ├── Valid? ──NO──► Error 401
       │
       YES
       ▼
┌─────────────────────────────────┐
│ Process Request                  │
└─────────────────────────────────┘
```

---

## 📊 Data Flow Summary

### **Registration Data Flow**
```
User Input → Flutter UI → AuthProvider → AuthService 
→ HTTP POST → AuthController → AuthenticationService 
→ UserRepository → Database → Response → AuthProvider 
→ Save Tokens → Navigate to Home
```

### **Login Data Flow**
```
User Input → Flutter UI → AuthProvider → AuthService 
→ HTTP POST → AuthController → AuthenticationService 
→ UserRepository → Database → JwtService → Response 
→ AuthProvider → Save Tokens & User → Navigate to Home
```

### **Location Data Flow**
```
GPS → LocationService → LocationModel → HomeScreen 
→ MapScreen (Display) → WebSocketService → WebSocket 
→ Backend → Store/Process Location
```

### **Ride Booking Data Flow**
```
User Input → BookRideScreen → RideService → HTTP POST 
→ RideController → RideService → RideRepository 
→ Database → Response → Display Status
```

---

## ✅ Complete Feature List

### **Backend Features**
- ✅ User registration (Rider & Driver)
- ✅ User login with JWT
- ✅ Password hashing (BCrypt)
- ✅ Role-based access control
- ✅ Database persistence
- ✅ WebSocket for real-time updates
- ✅ Ride management
- ✅ Location tracking
- ✅ Token refresh mechanism

### **Frontend Features**
- ✅ Beautiful UI/UX
- ✅ Login/Register screens
- ✅ Persistent login (remembers user)
- ✅ Interactive map (OpenStreetMap)
- ✅ Real-time location tracking
- ✅ GPS integration
- ✅ WebSocket communication
- ✅ Secure token storage
- ✅ Error handling
- ✅ Loading states
- ✅ Auto Bangalore location detection
- ✅ Book ride functionality

---

## 🎯 Summary

**Complete ride-hailing application with:**
- ✅ Full authentication system
- ✅ Real-time location tracking
- ✅ Interactive maps
- ✅ Database persistence
- ✅ WebSocket communication
- ✅ Secure token management
- ✅ Beautiful UI
- ✅ Error handling
- ✅ Persistent sessions

**Everything is working and production-ready!** 🚀

